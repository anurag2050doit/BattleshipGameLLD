# Application configuration
# Sensitive information such as credentials should be stored in environment variables or a secure secrets manager.
# Example:
# db.username=${DB_USERNAME}
# db.password=${DB_PASSWORD}
